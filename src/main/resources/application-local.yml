spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/weelink
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
app:
  link:
    domain: https://weel.ink
    alias-length: 5